// Math é um objeto que tem uma contnte chamada "PI" e uma funçao chamada pow" que seri potencia.

const raio = 5.6
const area = Math.PI * Math.pow(raio,2)

console.log(area.toFixed(2))
console.log(typeof Math)